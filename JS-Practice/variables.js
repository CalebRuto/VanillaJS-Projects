let admin, name;

name = 'John';  // Assigning a variable a value and transferring it to another variable
admin = name;
alert(admin);

let ourPlanet = 'earth';  //Create a variable with our planets name
let current_visitor = 'David'; //Create a variable for current visitor of a website